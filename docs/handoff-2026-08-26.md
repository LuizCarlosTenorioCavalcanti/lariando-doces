# Handoff — Lariando Doces, 2026-08-26 (3ª atualização: **v1 COMPLETA E PUBLICADA**)

## Onde está

**As 19 tasks do plano + renome + revisão final do branch: tudo completo.**
A revisão final (opus, branch inteiro) achou 1 Crítico + 6 Importantes + 6 Menores e
triou os 23 parqueados; a onda de correção fechou 12 achados em 4 commits, a re-revisão
pegou 1 regressão (rejeição com `null` no novo `naGavetas`), o conserto de uma linha foi
provado ao vivo e o round fechou limpo.

- Branch `main`, HEAD `de82b3d`, **empurrado** — o push dispara a publicação.
- Suíte: **203/203** verde (17 testes novos na onda). Build ok.
- Site: `https://luizcarlostenoriocavalcanti.github.io/lariando-doces/` (com `noindex`).
- Repo: `https://github.com/LuizCarlosTenorioCavalcanti/lariando-doces` (renomeado, ok).
- O workspace SDD (`.superpowers/sdd/2026-08-26-lariano-doces/`) pode ser apagado — o
  plano acabou e o registro agora é o git (a permissão bloqueou o `rm -rf` do agente).
  As rulings foram listadas na mensagem final da sessão de 2026-08-26 e os itens vivos
  estão em `docs/v1.1-backlog.md`.

## O que a onda final corrigiu (resumo)

1. **A folha de Ingredientes era inalcançável** — nenhum botão a abria (defeito do
   plano; nenhuma revisão de task podia ver). Agora: 3º link "Ingredientes" no rodapé
   + teste de fiação dos três links.
2. **Não havia como apagar um doce** — botão com confirmação na folha Editar doce.
3. Backup raso derrubava o app (receita sem `itens` → tela branca): validação profunda
   de `itens` + **error boundary** (`ParaquedasDeErro`) em volta do App.
4. Ordenação tolerante também em produções, com testes diretos nas 3 gavetas.
5. **Importação atômica**: uma transação só nas três gavetas — falha no meio reverte
   tudo (antes podia apagar sem repor a "única cópia").
6. `salvarProducao` recusa custo negativo.
7. Menores: key duplicada no detalhamento, botão salvar com unitário null, download de
   backup em mobile (appendChild + revoke adiado), erro+convite juntos, sw.js (só
   cacheia `ok`, fallback offline para a casca), README com a URL.

## O que ficou de fora, de propósito

- **Voltar do Android fecha o app** (e foco preso + `inert` na `Folha`) — real, a spec
  promete, mas é comportamento novo com superfície própria de bugs. É a primeira fatia
  do backlog v1.1 (seção 5 de `docs/v1.1-backlog.md`, que tem a triagem completa).
- Flake pré-existente da suíte sob paralelismo de arquivos do vitest (timeout ocasional
  de 5 s em teste pesado; reproduzido em HEAD intocado). Mitigação se incomodar:
  `--no-file-parallelism` ou `testTimeout` maior — local e/ou no CI.

## Pendências com o Luiz (não são código)

- **Checks manuais no celular** (Step 11 da Task 18): instalar na tela inicial, abrir em
  modo avião, cadastrar doce de verdade, mudar preço e ver produção salva não mudar,
  exportar/apagar/importar. Ninguém fez — é com ele e a esposa.
  **O celular da Lara (esposa) é iPhone**: instalar é Safari → Compartilhar →
  "Adicionar à Tela de Início"; o ícone vai sair feio até a v1.1 entregar o
  `apple-touch-icon` PNG (item no backlog); orientá-la a usar sempre pelo ícone
  instalado (protege o IndexedDB da limpeza de 7 dias do Safari) e manter backup.
- "Ainda está sem valor" da demonstração: nunca respondido; causa provável é ingrediente
  sem preço (o app avisa e mostra travessão por regra). Perguntar se voltar ao assunto.
- v1.1: começar por brainstorm/spec da margem bidirecional (item 1 do backlog) e da
  fatia "a folha se comporta como app" (item 5).

## Convenções (herdadas e confirmadas, valem para a v1.1)

- Import sem extensão: Vite resolve, Node puro não.
- Fake timers travam o `fake-indexeddb`.
- CI roda com `TZ: America/Sao_Paulo` (teste de `hojeLocal()` espera o dia de Brasília).
- Um implementador por vez em arquivos compartilhados; revisor é leitura, pode paralelo.
- Modelos: implementadores/revisores em sonnet; revisão final em opus.
- Todo despacho termina com as instruções de ponto do painel (CLAUDE.md global);
  zerar `_scratch/painel/estado.json` entre fatias.
- `gh` não existe na máquina; API pública do GitHub via curl para conferir workflow.
